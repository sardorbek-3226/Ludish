import { createSlice } from "@reduxjs/toolkit";

const userSlice = createSlice({
    name: "counter",
    initialState:{
        counter: 0,
        },
        reducers:{},
})
export default userSlice.reducer;